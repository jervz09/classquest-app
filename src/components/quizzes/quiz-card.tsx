import Link from "next/link";
import { ArrowRight, CircleCheck, CircleDashed, ListChecks } from "lucide-react";
export function QuizCard({ quiz }: { quiz: { id: string; title: string; subject: string | null; difficulty: string; game_mode: string; is_published: boolean; questions?: { count: number }[] } }) { return <Link href={`/teacher/quizzes/${quiz.id}`} className="group rounded-2xl border bg-card p-5 transition hover:-translate-y-0.5 hover:border-primary/40 hover:shadow-lg"><div className="flex items-start justify-between"><div><p className="text-sm font-medium capitalize text-primary">{quiz.subject || quiz.difficulty} · {quiz.game_mode.replace("_", " ")}</p><h2 className="mt-1 text-xl font-semibold">{quiz.title}</h2></div><ArrowRight className="text-muted-foreground transition group-hover:translate-x-1 group-hover:text-primary" /></div><div className="mt-6 flex gap-4 text-sm text-muted-foreground"><span className="flex items-center gap-1.5"><ListChecks className="size-4" />{quiz.questions?.[0]?.count ?? 0} questions</span><span className="flex items-center gap-1.5">{quiz.is_published ? <CircleCheck className="size-4 text-emerald-500" /> : <CircleDashed className="size-4" />}{quiz.is_published ? "Published" : "Draft"}</span></div></Link>; }
