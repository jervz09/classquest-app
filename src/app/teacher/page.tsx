import { BookOpen, Plus, Users } from "lucide-react";
export default function TeacherPage() { return <main className="mx-auto max-w-6xl px-5 py-10"><p className="font-medium text-primary">Teacher dashboard</p><h1 className="mt-2 text-4xl font-bold tracking-tight">Welcome to ClassQuest</h1><p className="mt-3 text-muted-foreground">Your account is ready. Classroom creation arrives in the next phase.</p><div className="mt-10 grid gap-4 sm:grid-cols-3">{[[Users,"Classes"],[BookOpen,"Quizzes"],[Plus,"Quick actions"]].map(([Icon,label]) => <section key={label as string} className="rounded-2xl border bg-card p-6"><Icon className="text-primary" /><h2 className="mt-4 font-semibold">{label as string}</h2><p className="mt-2 text-sm text-muted-foreground">Coming in the teacher workflow phase.</p></section>)}</div></main>; }
